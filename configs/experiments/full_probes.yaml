experiment_name: css_probes_full_github_only_v1
seed: 13
datasets:
  - data/css_pairs/role_1500.jsonl
  - data/css_pairs/neg_1500.jsonl
cache_root: cache
results_root: results
models:
  - bert-base-uncased
  - roberta-base
  - gpt2
layers:
  start: 0
  end_inclusive: 12
probe:
  seeds: [11, 13, 17, 19, 23]
  split_type: random
  test_size: 0.2
  dev_size: 0.1
  solver: liblinear
  max_iter: 300
  C: 1.0
